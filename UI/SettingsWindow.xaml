<Window x:Class="aydocs.NotchWin.UI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:aydocs.NotchWin.UI"
        mc:Ignorable="d"
        Title="NotchWin Settings" Height="600" Width="900" 
        WindowStyle="SingleBorderWindow" 
        Background="#121212"
        Foreground="White"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI Variable, Segoe UI, Arial">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Background="#1C1C1C" BorderBrush="#333" BorderThickness="0,0,1,0">
            <StackPanel Margin="0,20,0,0">
                <TextBlock Text="NotchWin" FontSize="20" FontWeight="Bold" Margin="20,0,20,20" Foreground="#5599FF"/>
                
                <ListBox x:Name="CategoryList" Background="Transparent" BorderThickness="0" SelectionChanged="CategoryList_SelectionChanged">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="Border" Padding="20,10" Background="Transparent" CornerRadius="8" Margin="10,2">
                                            <ContentPresenter />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="#333"/>
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="#2A2A2A"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBoxItem IsSelected="True">General</ListBoxItem>
                    <ListBoxItem>Appearance</ListBoxItem>
                    <ListBoxItem>Widgets</ListBoxItem>
                    <ListBoxItem>About</ListBoxItem>
                </ListBox>
            </StackPanel>
        </Border>

        <!-- Content Area -->
        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" Padding="40,30">
            <StackPanel x:Name="SettingsContent">
                <!-- Content will be swapped based on selection -->
            </StackPanel>
        </ScrollViewer>
        
        <!-- Save Button Bottom -->
        <Button Grid.Column="1" Content="Save Changes" HorizontalAlignment="Right" VerticalAlignment="Bottom" 
                Margin="20" Padding="20,10" Background="#5599FF" Foreground="White" BorderThickness="0"
                Click="SaveButton_Click">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5"/>
                </Style>
            </Button.Resources>
        </Button>
    </Grid>
</Window>
